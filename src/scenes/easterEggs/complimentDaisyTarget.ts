/**
 * The infinite compliment daisy Easter egg's position — same reasoning
 * as DAISY_GAME_POSITION in daisyGameTarget.ts, and deliberately placed
 * on the opposite side of the screen so the two never sit near each
 * other.
 */
export const COMPLIMENT_DAISY_POSITION = { left: '85%', bottom: '2px' };
